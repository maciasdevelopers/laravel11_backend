import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ComprasDevengacionServiciosComponent } from './compras_devengacion_servicios.component';

describe('ComprasDevengacionServiciosComponent', () => {
  let component: ComprasDevengacionServiciosComponent;
  let fixture: ComponentFixture<ComprasDevengacionServiciosComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ComprasDevengacionServiciosComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ComprasDevengacionServiciosComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
