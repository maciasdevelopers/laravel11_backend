import { NumeralFormatPipe } from './numeral-format.pipe';

describe('NumeralFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeralFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
